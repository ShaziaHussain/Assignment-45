// Define the make_shirt function
function make_shirt(size: string, message: string) {
    // Print a sentence summarizing the size and message
    console.log(`You've ordered a ${size} sized shirt with the message: "${message}".`);
}

// Call the make_shirt function with size and message arguments
make_shirt("medium", "I love JavaScript!");
